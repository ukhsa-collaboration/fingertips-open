@if (ViewBag.IsEnvironmentLive)
{
    // Force users to HTTPS on live site
    <script type="text/javascript">
        var href = window.location.href;
        if (href.indexOf('http:') === 0) {
            window.location.href = href.replace('http', 'https');
        }
    </script>
}