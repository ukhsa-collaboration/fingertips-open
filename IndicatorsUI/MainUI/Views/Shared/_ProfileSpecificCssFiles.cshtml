@using Profiles.MainUI.Models
@foreach (string file in (IEnumerable<string>)ViewBag.ExtraCssFiles)
{
    if (file[0] == '+')
    {
        @Html.Partial("_MapCss");
    }
    else
    {
    <link type="text/css" href="@(JsHelper.GetIncludePath(file, Model.CssPath))" rel="Stylesheet" />
    }
}